//INICIALIZACIÓN
event_inherited();

pagina = 0;      // Contador de página actual
paginas[0] = ""; //Arreglo de páginas

mostrar = false;  // Mostrar cartel por defecto
pagina = -1;     // Página actual, -1 = inactivo
